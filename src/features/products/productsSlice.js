import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const peoductsSlice = createSlice({
  name: "Product",
  initialState,
  reducers: {},
});

export const {} = peoductsSlice.actions;
export default peoductsSlice.reducer;
